import Cocoa

// What's the differnece between a struct and a tuple

var tuple = (name: String, age: Int, city: String).self

struct User {
    var name: String
    var age: Int
    var city: String
}


//

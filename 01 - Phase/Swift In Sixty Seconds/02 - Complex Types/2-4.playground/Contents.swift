import UIKit

// Arrays vs Sets vs Tuplues

let address = (house: 555, street: "Taylor Avenue", city: "Nashville") // use when need a precise position or name

    // use when need a collection of values need to be unqiue
let set = Set(["aardvark", "astronaut", "azalea"])

// use when need a cllection of dups or oder matters
let pythons = ["Eric", "Graham", "John", "Micheal", "Terry", "Terry"]

import Cocoa

// Creare a constant holding any temperature in Celsius
let celsiuse = 9
//  Converts that temperature to Fahrenheit by multiplying by 9, dividing by 5, then adding 32
let fahrenheit = ((celsiuse * 9) / 5) + 32
